感谢老师的教程，github功能很多很强大，暂时算是学了皮毛。
记了些命令
git config 
git init
git add
git commit -m <commit>
git status
git diff
git reset --hard commit.id
git log
git reflog
git checkout -- <file>
git reset HEAD <file>
git rm / git commit
git remote add origin git@github.com:githubID/repo-name.git
git push -u origin master 
git clone
git branch
git checkout -b <name>
git merge <name>
git branch -d <name>
git log --graph
git stash stash前要先add
git stash list
git stash apply/drop
git stash pop
git branch -D <name>
git remote -V
git push origin branch-name
git checkout -b branch-name origin/branch-name
git branch --set -upstream branch-name origin/branch-name
rebase(还没完全弄明白)
git tag <tag-name> commit_id
git tag
git show
git push origin <tag-name>/tags
git push origin :refs/tags/<tag-name>
 