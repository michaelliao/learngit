# Github pr test

testing pr.

---

Test:

```git
git init

git add <filename>
git commit -m "<message>"


git log [--oneline]
git status

git checkout
git switch <branch name>

git reset [--soft/hard/mixed]


git diff


git rm

git merge
git rebase

```
