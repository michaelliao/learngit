2.Git学习
完成情况：
1）Git简介：用途，优点，意义
2）Git安装：Linux系统，安装命令，配置
3）创建版本库：创建仓库（git init），添加文件至版本库（add,commit），多次add和add多个文件，查看版本日志(git log)
以上更详细内容已记录至word中。

Git在处理冲突的时候，如果本地文件和已经Push的文件冲突，要修改本地文件直到于push的一致，然后进行commit,接着再修改自己的修改？